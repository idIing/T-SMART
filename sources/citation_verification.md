# Citation verification table

Rows follow the manuscript in document order. Source excerpts are verbatim; bracketed ellipses are retained only where the batch extractor selected non-contiguous exact passages from the same source. The Notes column is intentionally blank.

| Claim (paper) | Source | Source excerpt | Notes |
|---|---|---|---|
| “Language models lose numerical precision when a time series is serialized into tokens, and on rigorous benchmarks their answers about such series lean on linguistic recall rather than computation.” | merrill24 | “human annotators outperform language models by a margin of up to thirty percentage points, with otherwise strong language models like GPT-4 barely doing better than random chance.” (p. 2) |  |
| “Language models lose numerical precision when a time series is serialized into tokens, and on rigorous benchmarks their answers about such series lean on linguistic recall rather than computation.” | gruver23 | “common tokenization methods like BPE tend to break a single number into tokens that don’t align with the digits, which can make arithmetic considerably more difficult.” (p. 4) |  |
| “Language models lose numerical precision when a time series is serialized into tokens, and on rigorous benchmarks their answers about such series lean on linguistic recall rather than computation.” | tan24 | “removing the LLM component or replacing it with a basic attention layer does not degrade forecasting performance—in most cases, the results even improve!” (p. 1) |  |
| “Merrill et al. find models marginally above chance on zero-shot time-series reasoning, exploiting linguistic signal more often than numerical signal.” | merrill24 | “otherwise strong language models like GPT-4 barely doing better than random chance.” (p. 2) |  |
| “Serializing a series into tokens inflates context and fragments numbers into unstable token chunks.” | gruver23 | “the number 42235630 gets tokenized as [422, 35, 630] by the GPT-3 tokenizer, and changes by even a single digit can result in an entirely different tokenization.” (p. 4) |  |
| “Tan et al. find that removing the language model from recent forecasting pipelines does not degrade them.” | tan24 | “removing the LLM component or replacing it with a basic attention layer does not degrade forecasting performance—in most cases, the results even improve!” (p. 1) |  |
| “Leading commercial models remain below two-thirds accuracy on TSAQA.” | jing26 | “best-performing commercial model, Gemini-2.5-Flash, achieves 65.08 average accuracy.” (p. 1) |  |
| “Leading commercial models remain below two-thirds accuracy on ARFBench.” | xie26 | “The top few-shot model on ARFBench, GPT-5, yielded a 62.7% accuracy and 51.9% F1.” (p. 9) |  |
| “Scaling improves perception while breaking down on prediction.” | yu26 | “Our findings reveal that: i) scaling laws hold for perception and reasoning but break down for prediction” (p. 1) |  |
| “TS-Agent keeps the series numeric and lets the model orchestrate analytical tools in a ReAct loop.” | liu25 | “TS-AGENT, an agentic, tool-grounded framework that uses LLMs strictly for iterative evidence-based reasoning, while delegating statistical and structural extraction to time series analytical tools operating on raw sequences. Our framework solves time series tasks through an evidence-driven agentic process: (1) it alternates between thinking, tool execution, and observation in a ReAct-style loop” (p. 1) |  |
| “TimeART trains an agent over a large tool corpus.” | wu26 | “we also collect a 100k expert trajectory corpus called TimeToolBench. To enhance TSRMs’ generalization capability, we then devise a four-stage training strategy” (p. 1) |  |
| “A recent survey organizes the area by reasoning topology, a spectrum of increasing complexity from direct reasoning to branch-structured exploration.” | chang26 | “This survey defines the problem and organizes the literature by reasoning topology with three families: direct reasoning in one step, linear chain reasoning with explicit intermediates, and branch-structured reasoning that explores, revises, and aggregates.” (p. 1) |  |
| “The first attempts to apply language models to time series serialized the series into text for forecasting.” | gruver23 | “By encoding time series as a string of numerical digits, we can frame time series forecasting as next-token prediction in text.” (p. 1) |  |
| “Merrill et al. find models near chance on zero-shot time-series reasoning, with accuracy leaning on linguistic shortcuts.” | merrill24 | “otherwise strong language models like GPT-4 barely doing better than random chance.” (p. 2) |  |
| “Tan et al. find the language model itself dispensable in forecasting pipelines.” | tan24 | “removing the LLM component or replacing it with a basic attention layer does not degrade forecasting performance—in most cases, the results even improve!” (p. 1) |  |
| “Tool-augmentation work relocates computation to external code.” | schick23 | “LMs can teach themselves to use external tools via simple APIs and achieve the best of both worlds.” (p. 1) |  |
| “Tool-augmentation work relocates computation to external code.” | gao23 | “uses the LLM to read natural language problems and generate programs as the intermediate reasoning steps, but offloads the solution step to a runtime such as a Python interpreter.” (p. 1) |  |
| “Tool-augmentation work relocates computation to external code.” | chen23pot | “the computation can be delegated to a program interpreter, which is used to execute the generated program, thus decoupling complex computation from reasoning and language understanding.” (p. 1) |  |
| “Moving the computation out of the model is the natural response, a premise established outside time series by tool-augmentation work that relocates computation to external code […] and by ReAct, which interleaves reasoning with tool calls.” | `yao23` — *ReAct: Synergizing Reasoning and Acting in Language Models* | “In this paper, we explore the use of LLMs to generate both reasoning traces and task-specific actions in an interleaved manner, allowing for greater synergy between the two: reasoning traces help the model induce, track, and update action plans as well as handle exceptions, while actions allow it to interface with and gather additional information from external sources such as knowledge bases or environments.” (PDF p. 1) |  |
| “A meta-analysis by Sprague et al. sharpens the premise: chain-of-thought gains concentrate on symbolic execution, where a dedicated solver outperforms the prompted chain.” | `sprague24` — *To CoT or Not to CoT? Chain-of-Thought Helps Mainly on Math and Symbolic Reasoning* | “Much of CoT’s gain comes from improving symbolic execution, but it underperforms relative to using a symbolic solver.” (PDF p. 1) |  |
| “TimeSeriesExam scores five reasoning categories and reports that models struggle most on causality.” | `cai24` — *TimeSeriesExam: A Time Series Understanding Exam* | “TimeSeriesExam, a configurable and scalable multiple-choice question exam designed to assess LLMs across five core time series understanding categories: pattern recognition, noise understanding, similarity analysis, anomaly detection, and causality analysis.” “Closed-source models such as GPT-4 and Gemini understand simple time series concepts significantly better than their open-source counterparts, while all models struggle with complex concepts such as causality analysis.” (PDF p. 1) |  |
| “MMTS-Bench adds out-of-distribution structure across four subsets, mixing multiple-choice with numerical answers on its Base subset under a 10% relative-tolerance metric, and finds that time-series-adapted LLMs lag general models on cross-domain generalization.” | `yin26` — *MMTS-Bench: A Comprehensive Benchmark for Time Series Understanding and Reasoning* | “MMTS-Bench comprises 2,424 time series question answering (TSQA) pairs across 4 subsets: Base, InWild, Match, and Align.” “TS-LLMs significantly lag behind general-purpose LLMs in cross-domain generalization.” (PDF p. 1) “Accuracy@10% provides a stricter binary evaluation criterion, determining whether the prediction falls within a 10% relative error tolerance.” (PDF p. 11) |  |
| “TSRBench reports that scaling helps perception but breaks down on prediction.” | yu26 | “scaling laws hold for perception and reasoning but break down for prediction” (p. 1) |  |
| “TSAQA caps the leading commercial models below two-thirds accuracy.” | jing26 | “best-performing commercial model, Gemini-2.5-Flash, achieves 65.08 average accuracy.” (p. 1) |  |
| “ARFBench caps the leading commercial models below two-thirds accuracy.” | xie26 | “The top few-shot model on ARFBench, GPT-5, yielded a 62.7% accuracy and 51.9% F1.” (p. 9) |  |
| “Of these benchmarks only ARFBench reports bootstrap confidence intervals.” | xie26 | “Bootstrapped 95% CIs resampled over 1000 batches of nbootstrap = 750 are provided for overall accuracy and F1.” (p. 9) |  |
| “TimeSage-MT shows sharp degradation on multi-turn, decision-oriented tasks.” | `kong26mt` — *TimeSage-MT: A Multi-Turn Benchmark for Evaluating Agentic Time Series Reasoning* | “Existing benchmarks focus on single-step tasks such as forecasting and anomaly detection, overlooking practical workflows where user goals evolve, agents must build on prior analyses, and conclusions emerge from accumulated evidence.” (PDF p. 1) “The full tier-wise results show that all models achieve their highest scores on L1 and drop at L2, with smaller changes from L2 to L4.” (PDF p. 33) |  |
| “Time-MQA […] extend[s] the format to open-ended and cross-modal questions.” | `kong25` — *Time-MQA: Time Series Multi-Task Question Answering with Context Enhancement* | “Time-MQA unifies ten time series analysis tasks across diverse domains (e.g., healthcare, finance, and energy) and five tasks (e.g., forecasting, anomaly detection, and open-ended reasoning question answering).” (PDF p. 2) |  |
| “[…] MTBench extend[s] the format to open-ended and cross-modal questions.” | `chen25` — *MTBench: A Multimodal Time Series Benchmark for Temporal Reasoning and Question Answering* | “To bridge this gap, we introduce Multimodal Time Series Benchmark (MTBench), a large-scale benchmark designed to evaluate large language models (LLMs) on the joint reasoning over time-series and text, exemplified through financial and weather domains.” “MTBench supports diverse tasks that require a deep understanding of both text and time-series data, including numerical forecasting, classification and news-driven question answering (QA).” (PDF p. 1) |  |
| “TS-Agent and TimeART instantiate the tool-grounding paradigm with evidence logs and tool corpora.” | liu25 | “records intermediate results in an explicit evidence log and corrects the reasoning trace via a self-refinement critic” (p. 1) |  |
| “TS-Agent's ablations remove its critic and its answer verification and report drops.” | liu25 | “the ablation results confirm that both the critic and the final answer verification are essential for TS-AGENT’s performance” (p. 8) |  |
| “TS-Agent and TimeART instantiate the tool-grounding paradigm with evidence logs and tool corpora.” | wu26 | “we also collect a 100k expert trajectory corpus called TimeToolBench.” (p. 1) |  |
| “Kim et al. take a complementary non-agentic route, compiling the series itself into a deterministic structured program before the model reasons.” | `kim26` — *Representing Time Series as Structured Programs for LLM Reasoning* | “In this work, we introduce Time-Series-to-Structured-Program representation (T2SP), a deterministic, training-free framework that transforms time series into a compact, structured program before LLM inference.” (PDF pp. 1–2) “we perform time-series editing, captioning, and question answering through a unified interface – without tool calls, agentic loops, or fine-tuning.” (PDF p. 2) |  |
| “In the taxonomy of Chang et al., T-SMART belongs to the direct-reasoning family: its router makes one categorical choice among tool families … and it does not explore or aggregate multiple reasoning paths within a run.” | chang26 | “Traditional time series analysis under direct reasoning treats the model as a one-shot mapper from temporal inputs (optionally with side context) to outputs such as forecasts, class labels, segmentation masks, or anomaly intervals. The execution topology is a single forward generation or completion without an explicit, multi-step trace.” (p. 10) |  |
| “Self-correction has a literature of its own, surveyed by Pan et al.” | `pan24correct` — *Automatically Correcting Large Language Models: Surveying the Landscape of Diverse Self-Correction Strategies* | “This paper presents a comprehensive review of this emerging class of techniques. We analyze and taxonomize a wide array of recent work utilizing these strategies, including training-time, generation-time, and post-hoc correction.” (PDF p. 1) |  |
| “Self-consistency improves chain-of-thought by sampling and voting; it is a decoding baseline, with no revision of an answer against feedback.” | `wang22` — *Self-Consistency Improves Chain of Thought Reasoning in Language Models* | “In this paper, we propose a new decoding strategy, self-consistency, to replace the naive greedy decoding used in chain-of-thought prompting. It first samples a diverse set of reasoning paths instead of only taking the greedy one, and then selects the most consistent answer by marginalizing out the sampled reasoning paths.” (PDF p. 1) |  |
| “Huang et al. show that intrinsic self-correction frequently fails without an external signal, while naming tool-verified correction as a regime their result leaves open.” | `huang24` — *Large Language Models Cannot Self-Correct Reasoning Yet* | “In the context of reasoning, our research indicates that LLMs struggle to self-correct their responses without external feedback, and at times, their performance even degrades after self-correction.” (PDF p. 1) “In contrast, external feedback incorporates inputs from humans, other models […] or external tools and knowledge sources.” (PDF p. 2) |  |
| “Rendering a series as an image can assist a vision-language model, yet that study’s own per-task results concentrate the benefit on perception tasks, show minimal gains on multi-step reasoning, and reverse outright on a tabular readiness task for one model family.” | `daswani24` — *Plots Unlock Time-Series Understanding in Multimodal Models* | “Our findings show that when using our plot-based approach multimodal models perform much better on tasks where the result is dependent on understanding the overall trend.” “On other tasks that require more advanced reasoning such as multi-step or connecting trend shapes with sequence magnitudes (e.g. identifying derivatives), and on tasks with tabular data (e.g. readiness assessment), the performance is equivalent.” (PDF p. 2) “Readiness (F1) 0 – -10.8 – 10.0” “Supplementary Table S1: Percent differences in median plot performances relative to median text performances.” (PDF p. 17) |  |
| “Multimodal fusion for time series frequently underperforms.” | yu26 | “despite the complementary nature of textual and visual forms of time series as inputs, current multimodal models fail to effectively fuse them for reciprocal performance” (p. 1) |  |
| “Conditional invocation has precedent outside time series: AutoTool learns by reinforcement when a visual tool call pays.” | `ma26` — *Are Tools Always Beneficial? Learning to Invoke Tools Adaptively for Dual-Mode Multimodal LLM Reasoning* | “To address this issue, we introduce AutoTool, a model that adaptively decides whether to invoke tools according to the characteristics of each query. Within a reinforcement learning framework, we design an explicit dual-mode reasoning strategy with mode-specific reward functions to guide the model toward producing accurate responses.” (PDF p. 1) |  |
| “AdaptMMBench benchmarks such mode selection.” | `zhang26adapt` — *AdaptMMBench: Benchmarking Adaptive Multimodal Reasoning for Mode Selection and Reasoning Process* | “Adaptive multimodal reasoning has emerged as a promising frontier in Vision-Language Models (VLMs), aiming to dynamically modulate between tool-augmented visual reasoning and text reasoning to enhance both effectiveness and efficiency.” (PDF p. 1) |  |
| “DePlot translates a plot to a table before a language model reasons over it.” | `deplot` — *DePlot: One-Shot Visual Language Reasoning by Plot-to-Table Translation* | “We decompose the challenge of visual language reasoning into two steps: (1) plot-to-text translation, and (2) reasoning over the translated text. The key in this method is a modality conversion module, named as DEPLOT, which translates the image of a plot or chart to a linearized table. The output of DEPLOT can then be directly used to prompt a pretrained large language model (LLM), exploiting the few-shot reasoning capabilities of LLMs.” (PDF p. 1) |  |
| “MatCha pretrains for chart derendering and math reasoning.” | `matcha` — *MatCha: Enhancing Visual Language Pretraining with Math Reasoning and Chart Derendering* | “We propose MATCHA (Math reasoning and Chart derendering pretraining) to enhance visual language models’ capabilities in jointly modeling charts/plots and language data. Specifically we propose several pretraining tasks that cover plot deconstruction and numerical reasoning which are the key capabilities in visual language modeling.” (PDF p. 1) |  |
| “PlotQA and ChartQA benchmark reading structure out of rendered charts.” | `plotqa` — *PlotQA: Reasoning over Scientific Plots* | “In this work, we aim to bridge this gap between existing datasets and real-world plots. Specifically, we propose PlotQA with 28.9 million question-answer pairs over 224,377 plots on data from real-world sources and questions based on crowd-sourced question templates.” (PDF p. 1) |  |
| “PlotQA and ChartQA benchmark reading structure out of rendered charts.” | `chartqa` — *ChartQA: A Benchmark for Question Answering about Charts with Visual and Logical Reasoning* | “In this work, we present a large-scale benchmark covering 9.6K human-written questions as well as 23.1K questions generated from human-written chart summaries. To address the unique challenges in our benchmark involving visual and logical reasoning over charts, we present two transformer-based models that combine visual features and the data table of the chart in a unified way to answer questions.” (PDF p. 1) |  |
| “Saqur et al. argue that reported gains in time-series modeling are often illusory once evaluation is taxonomy-aware.” | `saqur26` — *Seeking SOTA: Time-Series Forecasting Must Adopt Taxonomy-Specific Evaluation to Dispel Illusory Gains* | “We argue that the current practice of evaluating AI/ML time-series forecasting models — predominantly on benchmarks characterized by strong, persistent periodicities and seasonalities — obscures real progress by overlooking the performance of efficient, classical methods. We demonstrate that these ‘standard’ datasets often exhibit dominant auto-correlation patterns and seasonal cycles that can be effectively captured by simpler linear or statistical models, rendering complex deep-learning architectures frequently no more performant than their classical counterparts for these specific data characteristics.” (PDF p. 1) |  |
| “Language models process X as text and lose precision.” | merrill24 | “current language models fail to reason about these ubiquitous, critical data despite considerable human performance on the same tasks.” (p. 2) |  |
| “Language models process X as text and lose precision.” | gruver23 | “common tokenization methods like BPE tend to break a single number into tokens that don’t align with the digits, which can make arithmetic considerably more difficult.” (p. 4) |  |
| “Cai et al. report that every model in TimeSeriesExam struggles most on causality, the category in which a Granger test replaces a guess.” | `cai24` — *TimeSeriesExam: A Time Series Understanding Exam* | “Closed-source models such as GPT-4 and Gemini understand simple time series concepts significantly better than their open-source counterparts, while all models struggle with complex concepts such as causality analysis.” (PDF p. 1) “reasoning about causality, specifically Granger Causality [18] (causality).” (PDF p. 2) |  |
| “The term has one prior, mechanistically different use, for answer shifts toward an option pre-marked in the image itself.” | `lim24` — *Measuring Agreeableness Bias in Multimodal Models* | “This paper examines a phenomenon in multimodal language models where pre-marked options in question images can significantly influence model responses. Our study employs a systematic methodology to investigate this effect: we present models with images of multiple-choice questions, which they initially answer correctly, then expose the same model to versions with pre-marked options. Our findings reveal a significant shift in the models’ responses towards the pre-marked option, even when it contradicts their answers in the neutral settings.” (PDF p. 1) |  |
| “Related work studies VLM sycophancy under leading textual prompts.” | `zhao24` — *Sycophancy in Vision-Language Models: A Systematic Analysis and an Inference-Time Mitigation Framework* | “However, one critical issue that persists in these models is sycophancy, where models are unduly influenced by leading or deceptive prompts, resulting in biased outputs and hallucinations. […] We curate leading queries and quantify the susceptibility of state-of-the-art LVLMs to prompt-induced bias, revealing consistent performance degradation and instability across models and tasks.” (PDF p. 2) |  |
| “The design choice is contested in the literature, so we measure it.” | `daswani24` — *Plots Unlock Time-Series Understanding in Multimodal Models* | “Regardless, our main claim that plots are better suited than text as input to a multimodal model for time-series understanding holds true.” (PDF p. 3) |  |
| “The design choice [structured versus raw-pixel vision] is contested in the literature.” | yu26 | “visual representations outperform textual ones in Perception tasks, but this advantage diminishes” (p. 15) |  |
| “PAL … [has] the model emit arbitrary programs.” | gao23 | “uses the LLM to read natural language problems and generate programs as the intermediate reasoning steps” (p. 1) |  |
| “Program-of-Thoughts [has] the model emit arbitrary programs.” | chen23pot | “PoT, which uses language models (mainly Codex) to generate text and programming language statements, and finally an answer.” (p. 1) |  |
| “TS-Agent reports gpt-4o-mini.” | liu25 | “Across all experiments we use gpt-4o-mini to server as the backbone model for the reasoner and critic.” (p. 6) |  |
| “TimeSeriesExam, 746 multiple-choice questions across five categories, is the home surface.” | `cai24` — *TimeSeriesExam: A Time Series Understanding Exam* | “TimeSeriesExam, a configurable and scalable multiple-choice question exam designed to assess LLMs across five core time series understanding categories: pattern recognition, noise understanding, similarity analysis, anomaly detection, and causality analysis. TimeSeriesExam comprises of over 700 questions.” (PDF p. 1) |  |
| “MMTS-Bench, with four subsets, is the out-of-distribution surface, where Base free-response is scored by Eq. (4) and multiple choice by exact match.” | `yin26` — *MMTS-Bench: A Comprehensive Benchmark for Time Series Understanding and Reasoning* | “MMTS-Bench comprises 2,424 time series question answering (TSQA) pairs across 4 subsets: Base, InWild, Match, and Align.” (PDF p. 1) “Accuracy@10% provides a stricter binary evaluation criterion, determining whether the prediction falls within a 10% relative error tolerance.” (PDF p. 11) |  |
| “The protocol is paired throughout, following recommended practice for paired system comparisons.” | `dror18` — *The Hitchhiker’s Guide to Testing Statistical Significance in Natural Language Processing* | “McNemar’s test (McNemar, 1947) This test is designed for paired nominal observations (binary labels). The test is applied to a 2 × 2 contingency table, which tabulates the outcomes of two algorithms on a sample of n examples. […] That is, when applying both algorithms on the same data we would expect them to be correct/incorrect on the same proportion of items.” (proceedings pp. 1387–1388; PDF pp. 3–4) |  |
| “Significance uses the exact conditional McNemar test, conservative relative to mid-p variants.” | `fagerland13` — *The McNemar test for binary matched-pairs data: mid-p and asymptotic are better than exact conditional* | “The mid-p test performs well compared with the asymptotic, asymptotic with continuity correction, and exact conditional tests, and almost as good as the vastly more complex exact unconditional test. […] The exact conditional test and the asymptotic test with continuity correction did not perform well for any of the considered scenarios.” (abstract) |  |
| “API-level nondeterminism at temperature 0 is documented behavior.” | `ouyang23` — *An Empirical Study of the Non-determinism of ChatGPT in Code Generation* | “In addition, we find that setting the temperature to 0 does not guarantee determinism in code generation, although it indeed brings less non-determinism than the default configuration (temperature=1).” (PDF p. 1) |  |
| “Primary evidence throughout is the matched-backbone paired difference.” | `saqur26` — *Seeking SOTA: Time-Series Forecasting Must Adopt Taxonomy-Specific Evaluation to Dispel Illusory Gains* | “We call on the community to (I) retire or substantially augment current benchmarks with datasets exhibiting a wider spectrum of non-stationarities (e.g., structural breaks, time-varying volatility, concept drift) and less predictable dynamics drawn from diverse real-world domains, and (II) require every deep-learning submission to include robust classical and simple baselines, appropriately chosen for the specific characteristics (e.g., type of non-stationarity, seasonality) of the downstream task.” (PDF p. 1) |  |
| “Huang et al. show that intrinsic self-correction fails without external feedback and name tool-verified correction as the regime their result leaves open; these loops operate in that regime and still show no gain.” | `huang24` — *Large Language Models Cannot Self-Correct Reasoning Yet* | “LLMs struggle to self-correct their responses without external feedback, and at times, their performance even degrades after self-correction.” (PDF p. 1) “In contrast, external feedback incorporates inputs from humans, other models […] or external tools and knowledge sources.” (PDF p. 2) |  |
| “We include majority-vote self-consistency as a decoding-time control.” | `wang22` — *Self-Consistency Improves Chain of Thought Reasoning in Language Models* | “we sample a set of candidate outputs from the language model’s decoder, generating a diverse set of candidate reasoning paths.” “Unweighted sum (majority vote)” (PDF p. 3) |  |
| “TS-Agent's OA is the unweighted mean of the per-category scores … computed by us, since that paper reports no overall figure.” | liu25 | “Model Modality Pattern Rec. Noise Und. Anomaly Det. Similarity Causality” and “TS-Agent (Ours) 0.71 0.61 0.57 0.57 0.55” (Table 2, p. 7) |  |
| “Baseline rows reproduced from [TimeSeriesExam, TS-Agent]; T-SMART rows are this work, gemini-3.1, n=746.” | `cai24` — *TimeSeriesExam: A Time Series Understanding Exam* | “Figure 1: Accuracy of latest LLMs on the TimeSeriesExam. Closed-source LLMs outperform open-source ones in simple understanding tasks, but most models struggle with complex reasoning tasks.” (PDF p. 2) |  |
| “Baseline rows reproduced from [TS-Agent].” | liu25 | “Table 2. Comparison of model accuracy on TSExam dataset of time series understanding tasks (Cai et al., 2024).” (Table 2, p. 7) |  |
| “MMTS-Bench reports that chain-of-thought and multimodal fusion lift general models, so the null concerns bounded correction atop verified tool evidence rather than inference-time reasoning in general.” | `yin26` — *MMTS-Bench: A Comprehensive Benchmark for Time Series Understanding and Reasoning* | “chain-of-thought (CoT) reasoning and multimodal integration substantially improve performance.” (PDF p. 1) |  |
| “training-time adaptation also improves TS-QA, a lever the frozen backbone excludes by design.” | `kong25` — *Time-MQA: Time Series Multi-Task Question Answering with Context Enhancement* | “We demonstrate that fine-tuning LLMs on the TSQA dataset equips them with time series–specific knowledge, enabling natural language queries for comprehensive time series analysis.” (PDF p. 2) |  |
