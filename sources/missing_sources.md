# Missing sources — consolidated

Covers every cited work; the per-batch notes are in `missing_sources_a.md`,
`missing_sources_b.md`, and `missing_sources_c.md`.

None. All 36 cited works have a locally saved full-text PDF or accessible exact abstract record.

`dror18` is represented by its exact abstract plus the relevant verbatim full-text passage, and `fagerland13` by its exact abstract. Direct PDF downloads were blocked by the environment proxy in use at retrieval time; neither work lacked accessible text. See `fetch_manifest.md` for the per-key access type.
